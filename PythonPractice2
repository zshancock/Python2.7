## Program 2: Generates the circumference of a circle with a radius of 13 units and rounds to 3 places. ##

import math
C = round(2 * math.pi * 13, 3)
print "The circumference of a circle with a radius of 13 units is " + str(C) + " units."
