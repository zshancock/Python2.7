## A loop that allows user to enter names until user enters an empty string, then sorts the list and prints sorted
## list.                                                           

names = []

while True:
    name = raw_input("Please type a name (enter a space to quit):")
    if name == " ":
        break
    else:
        names.append(name)

print (', '.join(sorted(names))) 

## prints names sorted without ['']
## formatting.
